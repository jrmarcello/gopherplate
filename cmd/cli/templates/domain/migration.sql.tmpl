-- +goose Up
CREATE TABLE {{.DomainNamePlural}} (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    name VARCHAR(255) NOT NULL UNIQUE,
    created_at TIMESTAMP WITH TIME ZONE NOT NULL DEFAULT NOW(),
    updated_at TIMESTAMP WITH TIME ZONE NOT NULL DEFAULT NOW()
);

CREATE INDEX idx_{{.DomainNamePlural}}_created_at ON {{.DomainNamePlural}}(created_at DESC);

-- +goose Down
DROP INDEX IF EXISTS idx_{{.DomainNamePlural}}_created_at;
DROP TABLE IF EXISTS {{.DomainNamePlural}};
