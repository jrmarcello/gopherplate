apiVersion: apps/v1
kind: Deployment
metadata:
  name: gopherplate
spec:
  replicas: 2
  template:
    spec:
      containers:
        - name: gopherplate
          resources:
            requests:
              cpu: "300m"
              memory: "350Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
