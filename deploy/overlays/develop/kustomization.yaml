apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base
  - configmap.yaml
  - secret.yaml
  - kind-redis.yaml

namespace: gopherplate-dev

images:
  - name: 650155821999.dkr.ecr.sa-east-1.amazonaws.com/gopherplate
    newName: gopherplate # Imagem local
    newTag: dev

patches:
  - path: deployment-patch.yaml
    target:
      kind: Deployment
      name: gopherplate
  - path: ingress-patch.yaml
    target:
      kind: Ingress
      name: gopherplate
  - path: hpa-patch.yaml
    target:
      kind: HorizontalPodAutoscaler
      name: gopherplate
