name: go-boilerplate

services:
  # ============================================
  # DATABASE
  # ============================================
  ms-boilerplate-go-db:
    container_name: boilerplate-db
    image: postgres:16-alpine
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-user}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-password}
      POSTGRES_DB: ${POSTGRES_DB:-entities}
    volumes:
      - ms-boilerplate-go-db-data:/var/lib/postgresql/data
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-user} -d ${POSTGRES_DB:-entities}" ]
      interval: 5s
      timeout: 5s
      retries: 5
    restart: unless-stopped
    deploy:
      resources:
        limits:
          cpus: "0.5"
          memory: 256M

  # ============================================
  # REDIS (Cache)
  # ============================================
  ms-boilerplate-go-redis:
    container_name: boilerplate-redis
    image: redis:7-alpine
    ports:
      - "${REDIS_PORT:-6379}:6379"
    command: redis-server --appendonly yes
    volumes:
      - ms-boilerplate-go-redis-data:/data
    healthcheck:
      test: [ "CMD", "redis-cli", "ping" ]
      interval: 5s
      timeout: 3s
      retries: 5
    restart: unless-stopped
    deploy:
      resources:
        limits:
          cpus: "0.2"
          memory: 128M

volumes:
  ms-boilerplate-go-db-data:
    name: ms-boilerplate-go-db-data
  ms-boilerplate-go-redis-data:
    name: ms-boilerplate-go-redis-data

networks:
  default:
    name: ms-boilerplate-go-network
