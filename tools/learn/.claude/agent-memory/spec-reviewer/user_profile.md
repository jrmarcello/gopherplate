---
name: user-profile
description: Senior Go engineer working on gopherplate Clean Architecture microservice template; expects rigorous spec reviews with MUST FIX preferred over SHOULD FIX
metadata:
  type: user
---

Senior Go engineer building the gopherplate template (Clean Architecture microservice + CLI tooling). Works in the repo as both architect and reviewer.

Preferences observed:
- Applies quality-first maxim explicitly: "qualidade > velocidade > custo" — expects reviews to default to MUST FIX over SHOULD FIX when rigor is at stake.
- Understands Go idioms deeply (init() ordering, slog global state, package-level vars, TDD red/green/refactor cycle).
- Specs are treated as pre-code contracts; findings caught here prevent rework downstream.
- Expects findings to cite concrete file:line references and specific code evidence, not generalizations.
